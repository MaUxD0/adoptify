# TODO

- [ ] Fix git merge conflicts in client src files (App.tsx, main.tsx, routes/AppRouter.tsx)
- [ ] Ensure providers/router are mounted exactly once (no double BrowserRouter)
- [ ] Verify routes compile (React Router v6) and lazy imports are consistent
- [ ] Run TypeScript build/lint for client to confirm no remaining conflict markers
- [ ] Re-run search for conflict markers across repo

